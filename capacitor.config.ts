import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.japan',
  appName: 'Japan App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;